DROP TABLE retail_sample_data.ev_customers ;

CREATE MULTISET FOREIGN TABLE retail_sample_data.ev_customers , FALLBACK ,
     EXTERNAL SECURITY DEFINER TRUSTED DEMO_AUTH_NOS ,
     MAP = TD_MAP1
     (
      Id INTEGER NOT NULL,
      Gender VARCHAR(10) CHARACTER SET UNICODE,
      GivenName VARCHAR(40) CHARACTER SET UNICODE,
      MiddleInitial VARCHAR(1) CHARACTER SET UNICODE,
      Surname VARCHAR(50) CHARACTER SET UNICODE,
      StreetAddress VARCHAR(255) CHARACTER SET UNICODE,
      City VARCHAR(255) CHARACTER SET UNICODE,
      State VARCHAR(255) CHARACTER SET UNICODE,
      ZipCode INTEGER,
      Country VARCHAR(255) CHARACTER SET UNICODE,
      EmailAddress VARCHAR(255) CHARACTER SET UNICODE,
      Username VARCHAR(50) CHARACTER SET UNICODE,
      Passwd VARCHAR(50) CHARACTER SET UNICODE,
      TelephoneNumber VARCHAR(20) CHARACTER SET UNICODE,
      MothersMaiden VARCHAR(50) CHARACTER SET UNICODE,
      Birthday VARCHAR(20) CHARACTER SET UNICODE,
      CCType VARCHAR(20) CHARACTER SET UNICODE,
      CCNumber VARCHAR(40) CHARACTER SET UNICODE,
      CVV2 VARCHAR(4) CHARACTER SET UNICODE,
      CCExpires VARCHAR(20) CHARACTER SET UNICODE,
      NationalID VARCHAR(20) CHARACTER SET UNICODE,
      UPS VARCHAR(20) CHARACTER SET UNICODE,
      Occupation VARCHAR(50) CHARACTER SET UNICODE,
      Company VARCHAR(100) CHARACTER SET UNICODE,
      Vehicle VARCHAR(100) CHARACTER SET UNICODE,
      DomainName VARCHAR(100) CHARACTER SET UNICODE,
      BloodType VARCHAR(4) CHARACTER SET UNICODE,
      Pounds FLOAT,
      Kilograms VARCHAR(20) CHARACTER SET UNICODE,
      FeetInches VARCHAR(20) CHARACTER SET UNICODE,
      Centimeters FLOAT,
      GUID VARCHAR(100) CHARACTER SET UNICODE,
      Latitude FLOAT,
      Longitude FLOAT 
     )  
USING
(
      LOCATION  ('/s3/s3.amazonaws.com/alpha-data-store-td/retail_sample_data/EVCarBattery/customers.csv')
      MANIFEST  ('FALSE')
      ROWFORMAT  ('{"field_delimiter":",","record_delimiter":"\n","character_set":"LATIN"}')
      STOREDAS  ('TEXTFILE')
      HEADER  ('FALSE')
      STRIP_EXTERIOR_SPACES  ('FALSE')
      STRIP_ENCLOSING_CHAR  ('NONE')
)
NO PRIMARY INDEX ;