You must configure network settings before you can create elastic compute. This is a one-time setup per site.

You must have your identity provider and user provisioning set up before you can configure network settings.

Choose between **Pooled compute connections** and **Dedicated compute connections**. Choose the connectivity type and add the accounts to be used.

If you choose dedicated compute connections and select Use as default, that choice will carry over to the Create elastic compute section.

