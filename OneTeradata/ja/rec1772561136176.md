Teradataは以下のインデックス作成および検索アルゴリズムをサポートしています。

**FLAT**: ベクトル間の類似度または非類似度を計算します。

**IVF_FLAT**: 効率的な検索のためにデータをクラスタにグループ化します。

**HNSW**: グラフ ベースの探索を使用して、大規模なデータセットや大規模な環境で近似最近傍探索を可能にします。

**注**: HNSWは高速な検索結果を提供しますが、提供されるのは近似値です。

