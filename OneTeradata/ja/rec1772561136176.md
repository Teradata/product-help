Teradataは以下のインデックス作成および検索アルゴリズムをサポートしています:

**VectorDistance**: ベクトル間の類似度または非類似度を計算します。

**KMeans**: 効率的な検索のためにデータをクラスタにグループ化します。

**HNSW**: グラフベースの検索を使用して、大規模なデータセットや大規模な環境で高速な最近傍検索を可能にします。

**注**: HNSWは高速な検索結果を提供しますが、提供されるのは近似値です。

