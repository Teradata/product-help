カーネルとは、Notebook内のコードを実行するエンジンまたはインタプリタです。TeradataネイティブNotebookの各カーネルは、以下の言語をサポートしています。

-   Python


-   Teradata SQL


カーネル内では、以下の操作を行うことができます。

-   Notebookからコードを実行する


-   変数を管理し、セッションの状態を追跡する


-   コードの実行後に出力を生成する


セルを実行すると、カーネルがコードを解釈して実行し、その結果をNotebookに送り返します。

