Select the necessary database, pre-embedded data, database table, metadata columns, and key columns to pick the data for creating vector collection.

Ensure you have privileges to access the database.

