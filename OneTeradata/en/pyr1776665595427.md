You can import agents from Local Files by selecting the required Zip file and then uploading it. Ensure you provide the Agent Metadata before proceeding further.

**Note:** Only Zip files are allowed.

The following is an example of a package and its structure.

agent-package.zip

├── agent.py \# Required - Main agent entrypoint

├── config.yaml \# Required - Agent configuration

├── requirements.txt \# Required - Python dependencies

├── modules/ \# Optional - Additional Python modules

│ └── *.py

└── resources/ \# Optional - Additional resources

If an error occurs, check the details provided, and retry.

The common errors can include, server unavailable, incorrect credentials, and access issues.

