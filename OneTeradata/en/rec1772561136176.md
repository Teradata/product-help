Teradata supports these indexing and search algorithms:

**FLAT**: Computes similarity or dissimilarity between vectors.

**IVF_FLAT**: Groups data into clusters for efficient retrieval.

**HNSW**: Uses graph-based search for approximate nearest-neighbor retrieval in large datasets or at scale.

**Note**: HNSW provides fast search results, but with approximate values.

